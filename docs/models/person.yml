definitions:
  Person:
    type: object
    properties:
      id:
        type: integer
      slug:
        type: string
      name:
        type: string
      description:
        type: text
      birth:
        type: date
    required:
      - slug
      - name
      - description
      - birth
  PersonItems:
    type: object
    properties:
      total:
        type: integer
      perPage:
        type: integer
      page:
        type: integer
      lastPage:
        type: integer
      data:
        type: array
        items:
          $ref: "#/definitions/Person"
