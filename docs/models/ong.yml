definitions:
  Ong:
    type: object
    properties:
      id:
        type: integer
      slug:
        type: string
      name:
        type: string
      description:
        type: text
      birth:
        type: date
    required:
      - slug
      - name
      - description
      - birth
  OngItems:
    type: object
    properties:
      total:
        type: integer
      perPage:
        type: integer
      page:
        type: integer
      lastPage:
        type: integer
      data:
        type: array
        items:
          $ref: "#/definitions/Ong"
